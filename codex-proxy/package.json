{
  "name": "codex-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible proxy vers codex exec CLI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
